import Defaults

extension Defaults.Keys {
    static let confirmOnDelete = Key<Bool>("confirm-on-dekete", default: true)
    static let showOnAllSpaces = Key<Bool>("show-on-all-spaces", default: true)
}
